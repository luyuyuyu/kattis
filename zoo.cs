using System;

class zoo {
	public static void Main() {
		for (caseno = 1;; ++caseno) {
			var n = int.Parse(Console.ReadLine());
			if (n == 0) break;
			for (int i = 0; i < n; ++i) {
				var animal = Console.ReadLine().ToLower().Split();
				
			
